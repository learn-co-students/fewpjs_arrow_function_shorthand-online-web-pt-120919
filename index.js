let divide = () => {
    return 2000 / 100
}

let square = num => { // If your arrow function has only one parameter, the '()' become optional around the parameter
    return num * num
}

let add = (num1 , num2) => {
    return num1 + num2
}
